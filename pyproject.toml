[project]
name = "attention-to-detail"
version = "0.1.0"
description = "Manual calculation of every step in training a tiny transformer model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = []
